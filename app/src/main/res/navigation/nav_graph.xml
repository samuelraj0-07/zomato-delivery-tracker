<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/todayFragment">

    <fragment
        android:id="@+id/todayFragment"
        android:name="com.delivery.tracker.ui.today.TodayFragment"
        android:label="Today"/>

    <fragment
        android:id="@+id/historyFragment"
        android:name="com.delivery.tracker.ui.history.HistoryFragment"
        android:label="History"/>

    <fragment
        android:id="@+id/expensesFragment"
        android:name="com.delivery.tracker.ui.expenses.ExpensesFragment"
        android:label="Expenses"/>

    <fragment
        android:id="@+id/analyticsFragment"
        android:name="com.delivery.tracker.ui.analytics.AnalyticsFragment"
        android:label="Analytics"/>

</navigation>
